{
  "name": "@tripfinder/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo 'No lint configured yet'",
    "clean": "echo 'No clean needed'",
    "clean:build": "echo 'No clean:build needed'"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
